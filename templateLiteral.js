//the whole idea behind template literal is to shorten the concat and make it simple to use
//use back ticks ...back ticks are located right next to number 1 and above the tab 


let a = `good`;
let b = `${a} morning`;
console.log(b);
